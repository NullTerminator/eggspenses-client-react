import React, { Component } from 'react';

class EggChart extends Component {
  render() {
    return (
      <div id={ this.props.chart_id } style={{ height: '400px' }}>
      </div>
    );
  }
}

export default EggChart;
